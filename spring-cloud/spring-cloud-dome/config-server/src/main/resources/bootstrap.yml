server:
  port: 9393
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/yanghui0212/config
          username: yangqianghui1993@gmail.com
          password: 123456qqWW@@
          default-label: master
          basedir: D:\\java\\config-repo
eureka:
  instance:
    appname: config-server
    prefer-ip-address: true
    metadata-map:
      user.name: admin
      user.password: admin
  client:
    service-url:
      defaultZone: http://admin:admin@127.0.0.1:18761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "*"
      base-path: /actuator
  endpoint:
    health:
      show-details: always
